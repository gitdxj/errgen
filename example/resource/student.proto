syntax = "proto3";
package student;

message student {
    string name = 1;
    bool male = 2;
    repeated int32 scores = 3;
}

enum ErrStudent{
    // 注释1
    Err_Name_Cannot_Print = 0;
    // 注释2
    Err_Transgender = 1;
    // 注释3
    Err_Grade_Zero = 2;
}